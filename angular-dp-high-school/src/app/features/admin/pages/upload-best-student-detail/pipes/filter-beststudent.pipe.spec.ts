import { FilterBeststudentPipe } from './filter-beststudent.pipe';

describe('FilterBeststudentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBeststudentPipe();
    expect(pipe).toBeTruthy();
  });
});
