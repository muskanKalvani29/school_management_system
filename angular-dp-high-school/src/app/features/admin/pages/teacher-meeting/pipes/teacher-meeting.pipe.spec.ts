import { TeacherMeetingPipe } from './teacher-meeting.pipe';

describe('TeacherMeetingPipe', () => {
  it('create an instance', () => {
    const pipe = new TeacherMeetingPipe();
    expect(pipe).toBeTruthy();
  });
});
