import { FeeStructurePipe } from './fee-structure.pipe';

describe('FeeStructurePipe', () => {
  it('create an instance', () => {
    const pipe = new FeeStructurePipe();
    expect(pipe).toBeTruthy();
  });
});
